<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/color_background</item>
        <item name="colorPrimary">@color/spotify_streamer_primary</item>
        <item name="colorPrimaryDark">@color/spotify_streamer_primary_dark</item>
        <item name="colorAccent">@color/spotify_streamer_green</item>
    </style>

    <style name="ArtistList">
    </style>

</resources>
